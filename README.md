# DeepFake Detection Project

This project detects DeepFakes in images and videos using deep learning.

## Setup Instructions

1. Install requirements:

2. To detect an image:

3. To detect a video:

4. To generate Grad-CAM:

5. To run fairness testing:

## Project Structure
- `code/`: Source code
- `data/`: Sample data
- `docs/`: Report and slides
- `requirements.txt`: Pip install dependencies
- `environment.yml`: Conda environment
